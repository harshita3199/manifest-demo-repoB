apiVersion: v1
kind: Namespace
metadata:
  # Overridden per-PR by the ApplicationSet (kustomize.namespace = tess-pr-<N>).
  # Kept in base/ so the namespace is a tracked resource and gets pruned on PR close.
  name: tess-preview
